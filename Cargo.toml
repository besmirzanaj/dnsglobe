[package]
name = "dnsglobe"
version = "0.2.4"
edition = "2024"
rust-version = "1.85"
description = "Global DNS propagation checker TUI — watch a DNS record propagate across 39 public resolvers worldwide, on a world map in your terminal"
keywords = ["dns", "propagation", "tui", "terminal", "networking"]
categories = ["command-line-utilities", "network-programming"]
license = "MIT"
homepage = "https://github.com/besmirzanaj/dnsglobe"
repository = "https://github.com/besmirzanaj/dnsglobe"
readme = "README.md"

[dependencies]
anyhow = "1.0"
crossterm = { version = "0.29", features = ["event-stream"] }
futures = "0.3"
hickory-resolver = "0.26"
ratatui = "0.30"
tokio = { version = "1", features = ["full"] }

[profile.release]
lto = true
strip = true

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
